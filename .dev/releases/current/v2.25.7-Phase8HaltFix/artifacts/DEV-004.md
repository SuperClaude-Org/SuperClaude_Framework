---
id: DEV-004
severity: HIGH
status: backlog
assigned_command: "/sc:task-unified --compliance strict"
validated: true
validation_evidence: "IsolationLayers dataclass in src/superclaude/cli/sprint/executor.py (lines 97-124) already maps scoped_work_dir to CLAUDE_WORK_DIR as an env var. setup_isolation() (lines 141-173) instantiates with scoped_work_dir=config.release_dir. OQ-006 is already resolved by the codebase."
---

# DEV-004: Roadmap introduces unnecessary OQ-006 verification gate

## Deviation
The roadmap introduces an "OQ-006 Verification Gate" as a blocking Phase 1 prerequisite, treating the isolation mechanism (`CLAUDE_WORK_DIR` vs subprocess `cwd`) as unresolved. The spec's S3-R02 specifies `scoped_work_dir` directly without any open question.

## Validation Result
**CONFIRMED. OQ-006 is already resolved.** The `IsolationLayers` dataclass in `executor.py` (lines 97-124) maps `scoped_work_dir` to `CLAUDE_WORK_DIR` as an environment variable. `setup_isolation()` instantiates it with `config.release_dir`. The remaining work is S3-R07/S3-R08: wiring `env_vars` through `ClaudeProcess.__init__()` and `build_env()` in `src/superclaude/cli/pipeline/process.py`.

## Correction Task
In `config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/roadmap.md`:
1. Remove the OQ-006 verification gate from Phase 1 prerequisites
2. Replace the Phase 2 "Primary/Fallback" branching with a single implementation path using `CLAUDE_WORK_DIR` env var (as already implemented in `IsolationLayers`)
3. Remove the timeline contingency language ("contingent on OQ-006 resolution")
4. Add a note: "OQ-006 resolved: codebase uses `CLAUDE_WORK_DIR` env var via `IsolationLayers.scoped_work_dir`"

This is a documentation-only fix. No source code changes required.

## Acceptance Criteria
- [ ] No "OQ-006" blocking gate remains in Phase 1/Phase 2 instructions
- [ ] Phase 2 has a single implementation path (no Primary/Fallback branching)
- [ ] Timeline estimate no longer references OQ-006 contingency
- [ ] Resolution note documents the `CLAUDE_WORK_DIR` mechanism

## Files Affected
- `config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/roadmap.md`
