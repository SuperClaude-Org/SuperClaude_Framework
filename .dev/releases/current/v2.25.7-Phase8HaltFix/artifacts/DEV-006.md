---
id: DEV-006
severity: MEDIUM
status: backlog
assigned_command: "/sc:task-unified --compliance strict"
validated: true
validation_evidence: "Spec FIX-2-R02 requires config:SprintConfig with None default for backward compat. No deprecation warning specified. DiagnosticBundle in src/superclaude/cli/sprint/diagnostics.py (lines 31-49) has no config field yet — entire FIX-2 is unimplemented. Test T04.09 validates path resolution only, not warning emission."
---

# DEV-006: Roadmap adds deprecation warning beyond spec scope

## Deviation
The roadmap adds a deprecation warning when `DiagnosticBundle.config is None`, adopted from Variant A (Opus architect). The spec (FIX-2-R02) requires backward-compatible `None` default but says nothing about a deprecation warning.

## Validation Result
**CONFIRMED.** The spec requires only a `None` default for backward compatibility. The deprecation warning is a roadmap addition. No test covers warning emission. The `DiagnosticBundle` class currently has no `config` field at all.

## Correction Task
In `config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/roadmap.md`:
1. Remove the deprecation warning behavior from Phase 4 step 3
2. Remove deprecation warning references from milestone M4.3, Risk L2 mitigation, and OQ-004
3. If the deprecation warning is desired, create a separate follow-up task to add FIX-2-R03 to the PRD spec with an associated T04.xx test for warning emission

This is a documentation-only fix. No source code changes required.

## Acceptance Criteria
- [ ] No deprecation warning behavior in roadmap Phase 4 implementation steps
- [ ] Phase 4 aligns with FIX-2-R02: `config: SprintConfig | None = None` with fallback to hardcoded path
- [ ] If warning is desired, a separate backlog item exists with PRD amendment

## Files Affected
- `config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/roadmap.md`
