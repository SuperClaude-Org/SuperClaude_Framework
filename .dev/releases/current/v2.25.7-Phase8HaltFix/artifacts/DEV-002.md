---
id: DEV-002
severity: HIGH
status: backlog
assigned_command: "/sc:task-unified --compliance strict"
validated: true
validation_evidence: "SprintLogger is defined at line 12 of src/superclaude/cli/sprint/logging_.py with write_phase_result at line 88. No file named logger.py exists. All imports (executor.py, commands.py x2) import from .logging_, never .logger."
---

# DEV-002: Roadmap references wrong file for SprintLogger

## Deviation
The roadmap maps `SprintLogger.write_phase_result()` changes to `src/superclaude/cli/sprint/logger.py` instead of `src/superclaude/cli/sprint/logging_.py` as specified in the spec (FIX-1-R01).

## Validation Result
**CONFIRMED.** `SprintLogger` is defined at line 12 of `logging_.py` with `write_phase_result` at line 88. `logger.py` does not exist. The roadmap incorrectly references `logger.py` on lines 335 and 354.

## Correction Task
In `config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/roadmap.md`, replace all references to `logger.py` with `logging_.py` in:
1. The resource table (line ~335)
2. Phase 4 implementation instructions (line ~354)

This is a documentation-only fix. No source code changes required.

## Acceptance Criteria
- [ ] Zero occurrences of `logger.py` remain in `config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/roadmap.md` resource tables
- [ ] All `SprintLogger`/`write_phase_result` references point to `logging_.py`
- [ ] `grep -c "logger.py" config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/roadmap.md` returns 0 (in resource/file contexts)

## Files Affected
- `config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/roadmap.md`
