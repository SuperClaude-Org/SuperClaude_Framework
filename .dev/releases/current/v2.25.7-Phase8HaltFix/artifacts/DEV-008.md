---
id: DEV-008
severity: MEDIUM
status: backlog
assigned_command: "/sc:task-unified --compliance strict"
validated: true
validation_evidence: "uv run pytest tests/sprint/ --collect-only returns exactly 629 tests (baseline confirmed). Roadmap claims 629+10=>=638, but 629+10=639. Spec DoD-2 runs tests/ (full suite = 3213 tests) but threshold >=629 refers to sprint tests only, making the gate trivially passable."
dependency: "DEV-003"
---

# DEV-008: SC-002 total test count arithmetic error and scope mismatch

## Deviation
The roadmap's SC-002 expects >=638 total tests (claiming 629 baseline + 10 new). But 629+10=639, not 638. Additionally, the spec's DoD-2 runs `tests/` (full suite) but the >=629 threshold refers to sprint tests only.

## Validation Result
**CONFIRMED.** The 629 sprint baseline is accurate. The arithmetic is wrong (639 not 638). The DoD-2 command scope (`tests/` = 3213 tests) vs threshold scope (sprint = 629) makes the gate trivially passable even with significant regressions outside sprint tests.

## Correction Task
1. In `config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/roadmap.md`, fix SC-002:
   - If T04.10 kept (DEV-003 accepted): ">=639 sprint tests passed (629 baseline + 10 new)"
   - If T04.10 removed: ">=638 sprint tests passed (629 baseline + 9 new)"
2. In `config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/v2.25.7-phase8-sprint-context-resilience-prd.md`, clarify DoD-2:
   - Change verification command to `uv run pytest tests/sprint/ -v --tb=short` (scope to sprint tests)
   - OR change threshold to reflect full suite count with sprint-specific sub-threshold

## Acceptance Criteria
- [ ] SC-002 arithmetic is correct (baseline + new = stated threshold)
- [ ] DoD-2 verification command scope matches the stated threshold scope
- [ ] Consistent with DEV-003 resolution (9 or 10 new tests)

## Files Affected
- `config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/roadmap.md`
- `config/workspace/IronClaude/.dev/releases/current/v2.25.7-Phase8HaltFix/v2.25.7-phase8-sprint-context-resilience-prd.md`
