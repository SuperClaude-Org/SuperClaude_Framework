# D-0016: Ambiguity Detection Evidence

- When target matches both command name and skill name, command-first policy applied
- Resolves as COMMAND_NAME with skill linked via ## Activation parsing
- Verified with tmp filesystem test
