# Phase 3 -- Core Pipeline and Executor

Build the sequential execution backbone and establish baseline observability infrastructure before any Claude-assisted logic. Observability is a Phase 3 prerequisite because long-running Claude steps require execution traces for failure diagnosis.

### T03.01 -- Implement Core Domain Models in models.py

| Field | Value |
|---|---|
| Roadmap Item IDs | R-026 |
| Why | All pipeline components depend on domain models: PortifyPhaseType, ConvergenceState, PortifyConfig, PortifyStep, PortifyStepResult, PortifyOutcome, PortifyStatus, MonitorState, TurnLedger. |
| Effort | L |
| Risk | Medium |
| Risk Drivers | model, schema, system-wide |
| Tier | STRICT |
| Confidence | [█████████░] 90% |
| Requires Confirmation | No |
| Critical Path Override | Yes |
| Verification Method | Sub-agent (quality-engineer) |
| MCP Requirements | Required: Sequential, Serena | Preferred: Context7 |
| Fallback Allowed | No |
| Sub-Agent Delegation | Required |
| Deliverable IDs | D-0012 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0012/spec.md

**Deliverables:**
- Complete domain models in `src/superclaude/cli/cli_portify/models.py`: `PortifyPhaseType` (enum), `ConvergenceState` (enum), `PortifyConfig`, `PortifyStep`, `PortifyStepResult`, `PortifyOutcome`, `PortifyStatus`, `MonitorState`, `TurnLedger`

**Steps:**
1. **[PLANNING]** Review base type interfaces from T01.01 (PipelineConfig, Step, StepResult) for inheritance contracts
2. **[PLANNING]** Review OQ-001 resolution from T01.02 for TurnLedger semantics
3. **[EXECUTION]** Implement `PortifyPhaseType` enum with values: PREREQUISITES, ANALYSIS, USER_REVIEW, SPECIFICATION, SYNTHESIS, CONVERGENCE
4. **[EXECUTION]** Implement `ConvergenceState` enum: NOT_STARTED, REVIEWING, INCORPORATING, SCORING, CONVERGED, ESCALATED
5. **[EXECUTION]** Implement remaining dataclasses extending framework base types per AC-003
6. **[EXECUTION]** Implement `TurnLedger` as executor-internal class per OQ-001 resolution
7. **[VERIFICATION]** Write unit tests: all models instantiate correctly, enums have expected members, base type inheritance verified
8. **[COMPLETION]** Document model hierarchy and relationships in D-0012/spec.md

**Acceptance Criteria:**
- All 9 domain models defined in `src/superclaude/cli/cli_portify/models.py` and importable
- `PortifyPhaseType` and `ConvergenceState` are enums with all specified values
- Domain models extend framework base types (PipelineConfig, Step, StepResult) per AC-003, NFR-016
- Model hierarchy documented in .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0012/spec.md

**Validation:**
- `uv run pytest tests/ -k "test_domain_models"` exits 0
- Evidence: linkable artifact produced at D-0012/spec.md

**Dependencies:** T01.04, T02.07
**Rollback:** TBD (if not specified in roadmap)

---

### T03.02 -- Implement __init__.py with Generation Markers

| Field | Value |
|---|---|
| Roadmap Item IDs | R-027 |
| Why | AC-013 requires __init__.py to contain Generated by / Portified from markers for collision detection in Phase 2. |
| Effort | XS |
| Risk | Low |
| Risk Drivers | None matched |
| Tier | STANDARD |
| Confidence | [███████░░░] 78% |
| Requires Confirmation | No |
| Critical Path Override | No |
| Verification Method | Direct test execution |
| MCP Requirements | Preferred: Sequential |
| Fallback Allowed | Yes |
| Sub-Agent Delegation | None |
| Deliverable IDs | D-0013 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0013/evidence.md

**Deliverables:**
- `src/superclaude/cli/cli_portify/__init__.py` with `Generated by` / `Portified from` marker comments per AC-013

**Steps:**
1. **[PLANNING]** Review marker format confirmed in T01.03
2. **[PLANNING]** Check existing `__init__.py` patterns in `src/superclaude/cli/`
3. **[EXECUTION]** Create `__init__.py` with `# Generated by superclaude cli-portify` and `# Portified from <skill_name>` markers
4. **[EXECUTION]** Add module exports for public API symbols
5. **[VERIFICATION]** Verify marker strings are detectable by collision detection logic from T02.03
6. **[COMPLETION]** Document marker format in D-0013/evidence.md

**Acceptance Criteria:**
- `__init__.py` at `src/superclaude/cli/cli_portify/__init__.py` contains both `Generated by` and `Portified from` marker strings
- Markers are detectable by the collision detection logic implemented in T02.03
- File follows existing `src/superclaude/cli/` module conventions
- Marker format documented in .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0013/evidence.md

**Validation:**
- Manual check: `grep -c "Generated by\|Portified from" src/superclaude/cli/cli_portify/__init__.py` returns 2
- Evidence: linkable artifact produced at D-0013/evidence.md

**Dependencies:** T03.01
**Rollback:** TBD (if not specified in roadmap)

---

### T03.03 -- Implement Step Registration in Mandated Order

| Field | Value |
|---|---|
| Roadmap Item IDs | R-028 |
| Why | NFR-006 and AC-012 require steps registered in mandated module generation order for deterministic pipeline execution. |
| Effort | S |
| Risk | Low |
| Risk Drivers | None matched |
| Tier | STANDARD |
| Confidence | [███████░░░] 78% |
| Requires Confirmation | No |
| Critical Path Override | No |
| Verification Method | Direct test execution |
| MCP Requirements | Preferred: Sequential |
| Fallback Allowed | Yes |
| Sub-Agent Delegation | None |
| Deliverable IDs | D-0014 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0014/evidence.md

**Deliverables:**
- Step registration logic enforcing mandated order: models → gates → prompts → config → inventory → monitor → process → executor → tui → logging_ → diagnostics → commands → __init__

**Steps:**
1. **[PLANNING]** Review mandated module generation order from roadmap NFR-006
2. **[PLANNING]** Confirm step registration mechanism in executor skeleton
3. **[EXECUTION]** Implement ordered step registration list matching: models, gates, prompts, config, inventory, monitor, process, executor, tui, logging_, diagnostics, commands, __init__
4. **[EXECUTION]** Add assertion that step registration order cannot be modified at runtime
5. **[VERIFICATION]** Write unit test asserting step registration matches mandated order exactly
6. **[COMPLETION]** Document step order rationale in D-0014/evidence.md

**Acceptance Criteria:**
- Steps registered in exact mandated order: models → gates → prompts → config → inventory → monitor → process → executor → tui → logging_ → diagnostics → commands → __init__
- Registration order is immutable at runtime (assertion or frozen data structure)
- Order matches NFR-006 and AC-012 specifications exactly
- Step order documented in .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0014/evidence.md

**Validation:**
- `uv run pytest tests/ -k "test_step_order"` exits 0
- Evidence: linkable artifact produced at D-0014/evidence.md

**Dependencies:** T03.01
**Rollback:** TBD (if not specified in roadmap)

---

### T03.04 -- Implement Executor with Sequential Execution, Dry-Run, Resume, and Signal Handling

| Field | Value |
|---|---|
| Roadmap Item IDs | R-029, R-030 |
| Why | The executor is the pipeline backbone: sequential-only execution, --dry-run filtering to PREREQUISITES/ANALYSIS/USER_REVIEW/SPECIFICATION phase types, --resume <step-id> with skip logic, and signal handling foundation. |
| Effort | XL |
| Risk | High |
| Risk Drivers | system-wide, performance, end-to-end |
| Tier | STRICT |
| Confidence | [█████████░] 92% |
| Requires Confirmation | No |
| Critical Path Override | Yes |
| Verification Method | Sub-agent (quality-engineer) |
| MCP Requirements | Required: Sequential, Serena | Preferred: Context7 |
| Fallback Allowed | No |
| Sub-Agent Delegation | Required |
| Deliverable IDs | D-0015 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0015/spec.md

**Deliverables:**
- `src/superclaude/cli/cli_portify/executor.py` with sequential execution loop, `--dry-run` phase type filtering, `--resume <step-id>` skip logic, and signal handling integration points

**Steps:**
1. **[PLANNING]** Review domain models from T03.01 for PortifyStep, PortifyStepResult, PortifyOutcome, PortifyStatus
2. **[PLANNING]** Map executor flow: load steps → filter (dry-run) → iterate → execute → classify result → checkpoint
3. **[EXECUTION]** Implement sequential execution loop iterating registered steps in order
4. **[EXECUTION]** Implement `--dry-run` filtering: only execute steps with phase type in {PREREQUISITES, ANALYSIS, USER_REVIEW, SPECIFICATION}
5. **[EXECUTION]** Implement `--resume <step-id>`: skip all steps before the specified step-id, preserve completed step results
6. **[EXECUTION]** Add signal handling integration points (SIGINT/SIGTERM hooks for T03.09)
7. **[VERIFICATION]** Write unit test: assert dry-run filters to correct phase types only (SC-012 early validation per roadmap 4a)
8. **[COMPLETION]** Document executor architecture in D-0015/spec.md

**Acceptance Criteria:**
- `executor.py` implements sequential step execution loop processing steps in registered order
- `--dry-run` filters execution to PREREQUISITES, ANALYSIS, USER_REVIEW, SPECIFICATION phase types only (SC-012)
- `--resume <step-id>` skips completed steps and resumes from specified step with prior results preserved
- Unit test asserting dry-run phase type filtering passes via `uv run pytest`

**Validation:**
- `uv run pytest tests/ -k "test_executor"` exits 0
- Evidence: linkable artifact produced at D-0015/spec.md

**Dependencies:** T03.01, T03.03
**Rollback:** TBD (if not specified in roadmap)
**Notes:** XL effort — subtasks: sequential loop, dry-run filter, resume logic, signal hooks. Roadmap explicitly couples dry-run unit test (4a) with executor implementation.

---

### Checkpoint: Phase 3 / Tasks T03.01-T03.04

**Purpose:** Verify core domain models, step registration, and executor skeleton are functional before proceeding to process management and observability.

**Checkpoint Report Path:** .dev/releases/current/v2.25-cli-portify-cli/checkpoints/CP-P03-T01-T04.md

**Verification:**
- All 9 domain models instantiate correctly with proper base type inheritance
- Step registration order matches mandated sequence exactly
- Executor sequential loop, dry-run filtering, and resume logic function correctly

**Exit Criteria:**
- Domain models, step registration, and executor skeleton pass all unit tests
- Dry-run phase type filtering validated per SC-012
- No blocking issues for process management tasks (T03.05-T03.12)

---

### T03.05 -- Implement Claude Binary Detection via shutil.which

| Field | Value |
|---|---|
| Roadmap Item IDs | R-031 |
| Why | FR-036 and AC-008 require claude CLI binary detection as a hard runtime dependency with no fallback. Pipeline must fail early if claude is not found. |
| Effort | S |
| Risk | Medium |
| Risk Drivers | depends (hard runtime dependency) |
| Tier | STRICT |
| Confidence | [████████░░] 85% |
| Requires Confirmation | No |
| Critical Path Override | No |
| Verification Method | Sub-agent (quality-engineer) |
| MCP Requirements | Required: Sequential, Serena |
| Fallback Allowed | No |
| Sub-Agent Delegation | None |
| Deliverable IDs | D-0016 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0016/evidence.md

**Deliverables:**
- Claude binary detection in `src/superclaude/cli/cli_portify/process.py` using `shutil.which("claude")` per FR-036, NFR-017

**Steps:**
1. **[PLANNING]** Confirm D-001 dependency: claude CLI binary is a hard runtime dependency with no fallback
2. **[PLANNING]** Review NFR-017 for minimum version requirements
3. **[EXECUTION]** Implement `shutil.which("claude")` detection in `process.py`
4. **[EXECUTION]** Raise clear error with installation instructions if binary not found
5. **[VERIFICATION]** Write unit tests: binary found (mock), binary not found (raises error with message)
6. **[COMPLETION]** Document detection logic and error messaging in D-0016/evidence.md

**Acceptance Criteria:**
- `process.py` uses `shutil.which("claude")` to detect the claude CLI binary
- Pipeline raises a clear error with installation guidance when claude binary is not found
- Detection runs early in pipeline initialization (before any Claude-assisted steps)
- Detection logic documented in .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0016/evidence.md

**Validation:**
- `uv run pytest tests/ -k "test_claude_binary"` exits 0
- Evidence: linkable artifact produced at D-0016/evidence.md

**Dependencies:** T03.04
**Rollback:** TBD (if not specified in roadmap)

---

### T03.06 -- Implement Timeout Classification and _determine_status()

| Field | Value |
|---|---|
| Roadmap Item IDs | R-032, R-033 |
| Why | NFR-018 requires exit code 124 → TIMEOUT classification, other non-zero → ERROR. _determine_status() must classify based on exit code + EXIT_RECOMMENDATION + artifact presence. |
| Effort | M |
| Risk | Medium |
| Risk Drivers | performance (latency classification) |
| Tier | STRICT |
| Confidence | [████████░░] 88% |
| Requires Confirmation | No |
| Critical Path Override | No |
| Verification Method | Sub-agent (quality-engineer) |
| MCP Requirements | Required: Sequential, Serena |
| Fallback Allowed | No |
| Sub-Agent Delegation | Recommended |
| Deliverable IDs | D-0017 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0017/spec.md

**Deliverables:**
- Timeout classification (exit 124 → TIMEOUT, other non-zero → ERROR) and `_determine_status()` method in `executor.py` classifying outcomes based on exit code, EXIT_RECOMMENDATION marker, and artifact presence

**Steps:**
1. **[PLANNING]** Map all exit code → status mappings: 0 → PASS, 124 → TIMEOUT, other → ERROR
2. **[PLANNING]** Define _determine_status() decision tree: exit code → EXIT_RECOMMENDATION check → artifact presence check
3. **[EXECUTION]** Implement timeout classification: exit code 124 → TIMEOUT status per NFR-018
4. **[EXECUTION]** Implement `_determine_status()`: classify PASS, PASS_NO_SIGNAL, PASS_NO_REPORT, TIMEOUT, ERROR based on exit code + EXIT_RECOMMENDATION + artifact presence
5. **[VERIFICATION]** Write exhaustive unit tests: all status paths including PASS_NO_SIGNAL (triggers retry) vs PASS_NO_REPORT (no retry)
6. **[COMPLETION]** Document status classification decision tree in D-0017/spec.md

**Acceptance Criteria:**
- Exit code 124 classified as TIMEOUT per NFR-018 (SC-014)
- `_determine_status()` correctly classifies: PASS (exit 0 + marker + artifact), PASS_NO_SIGNAL (exit 0 + no marker + artifact), PASS_NO_REPORT (exit 0 + artifact, no result file), TIMEOUT (exit 124), ERROR (other non-zero)
- PASS_NO_SIGNAL triggers retry; PASS_NO_REPORT does not trigger retry (per roadmap note)
- Classification documented in .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0017/spec.md

**Validation:**
- `uv run pytest tests/ -k "test_determine_status"` exits 0
- Evidence: linkable artifact produced at D-0017/spec.md

**Dependencies:** T03.04
**Rollback:** TBD (if not specified in roadmap)

---

### T03.07 -- Implement Retry Mechanism with retry_limit=1

| Field | Value |
|---|---|
| Roadmap Item IDs | R-034 |
| Why | NFR-002 requires retry_limit=1 for Claude-assisted steps to provide one recovery cycle on transient failures. |
| Effort | M |
| Risk | Medium |
| Risk Drivers | performance (retry behavior) |
| Tier | STRICT |
| Confidence | [████████░░] 85% |
| Requires Confirmation | No |
| Critical Path Override | No |
| Verification Method | Sub-agent (quality-engineer) |
| MCP Requirements | Required: Sequential, Serena |
| Fallback Allowed | No |
| Sub-Agent Delegation | None |
| Deliverable IDs | D-0018 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0018/spec.md

**Deliverables:**
- Retry mechanism in `executor.py` with `retry_limit=1` for Claude-assisted steps, triggered by PASS_NO_SIGNAL status

**Steps:**
1. **[PLANNING]** Review _determine_status() outputs from T03.06 to identify retry-triggering statuses
2. **[PLANNING]** Confirm PASS_NO_SIGNAL triggers retry but PASS_NO_REPORT does not
3. **[EXECUTION]** Implement retry wrapper: on PASS_NO_SIGNAL, re-execute step up to retry_limit=1
4. **[EXECUTION]** Track retry count per step; emit retry event to execution log
5. **[VERIFICATION]** Write unit tests: retry triggered on PASS_NO_SIGNAL, not triggered on PASS_NO_REPORT, max 1 retry enforced
6. **[COMPLETION]** Document retry semantics in D-0018/spec.md

**Acceptance Criteria:**
- Retry mechanism executes at most 1 additional attempt (retry_limit=1) for Claude-assisted steps
- Retry triggered by PASS_NO_SIGNAL status (result file present, no EXIT_RECOMMENDATION marker)
- No retry on PASS_NO_REPORT status (artifact produced, no result file)
- Retry semantics documented in .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0018/spec.md

**Validation:**
- `uv run pytest tests/ -k "test_retry"` exits 0
- Evidence: linkable artifact produced at D-0018/spec.md

**Dependencies:** T03.06
**Rollback:** TBD (if not specified in roadmap)

---

### T03.08 -- Implement TurnLedger with can_launch() and Budget Exhaustion

| Field | Value |
|---|---|
| Roadmap Item IDs | R-035 |
| Why | FR-040 requires TurnLedger to track turn budget, check can_launch() before each step, and produce HALTED outcome on budget exhaustion. |
| Effort | M |
| Risk | Medium |
| Risk Drivers | performance (budget tracking) |
| Tier | STRICT |
| Confidence | [████████░░] 88% |
| Requires Confirmation | No |
| Critical Path Override | No |
| Verification Method | Sub-agent (quality-engineer) |
| MCP Requirements | Required: Sequential, Serena |
| Fallback Allowed | No |
| Sub-Agent Delegation | Recommended |
| Deliverable IDs | D-0019 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0019/spec.md

**Deliverables:**
- `TurnLedger` class in `models.py` with `can_launch()` pre-step check and budget exhaustion → HALTED outcome

**Steps:**
1. **[PLANNING]** Review OQ-001 resolution from T01.02 for TurnLedger semantics (what constitutes "one turn")
2. **[PLANNING]** Confirm TurnLedger is executor-internal class (not spec-defined dataclass)
3. **[EXECUTION]** Implement TurnLedger: track total budget, consumed turns, remaining turns
4. **[EXECUTION]** Implement `can_launch()`: return False when remaining turns insufficient for next step
5. **[EXECUTION]** Wire budget exhaustion to HALTED outcome in executor
6. **[VERIFICATION]** Write unit tests: budget tracking, can_launch() returns False at exhaustion, HALTED outcome emitted
7. **[COMPLETION]** Document TurnLedger semantics in D-0019/spec.md

**Acceptance Criteria:**
- `TurnLedger` tracks total budget, consumed turns, and remaining turns
- `can_launch()` returns False when budget insufficient, triggering HALTED outcome (FR-040)
- Budget exhaustion mid-pipeline produces correct HALTED status in return contract
- TurnLedger semantics documented in .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0019/spec.md

**Validation:**
- `uv run pytest tests/ -k "test_turn_ledger"` exits 0
- Evidence: linkable artifact produced at D-0019/spec.md

**Dependencies:** T03.01
**Rollback:** TBD (if not specified in roadmap)

---

### T03.09 -- Implement Signal Handlers for SIGINT/SIGTERM

| Field | Value |
|---|---|
| Roadmap Item IDs | R-036 |
| Why | FR-039 and NFR-003 require SIGINT/SIGTERM handlers that complete the current step result, then set INTERRUPTED outcome for graceful shutdown. |
| Effort | M |
| Risk | High |
| Risk Drivers | security (signal handling), system-wide |
| Tier | STRICT |
| Confidence | [█████████░] 90% |
| Requires Confirmation | No |
| Critical Path Override | No |
| Verification Method | Sub-agent (quality-engineer) |
| MCP Requirements | Required: Sequential, Serena |
| Fallback Allowed | No |
| Sub-Agent Delegation | Required |
| Deliverable IDs | D-0020 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0020/spec.md

**Deliverables:**
- Signal handlers in `process.py` for SIGINT and SIGTERM that complete current step result and set INTERRUPTED outcome

**Steps:**
1. **[PLANNING]** Review OQ-002 resolution from T01.02 for kill signal mechanism (SIGTERM→SIGKILL vs direct)
2. **[PLANNING]** Map signal handling flow: signal received → set interrupt flag → complete current step → emit INTERRUPTED
3. **[EXECUTION]** Register SIGINT and SIGTERM handlers using Python `signal` module
4. **[EXECUTION]** Implement graceful shutdown: allow current step to complete its result, then set INTERRUPTED outcome
5. **[EXECUTION]** Ensure return-contract.yaml is emitted on INTERRUPTED path (coordinate with T03.10)
6. **[VERIFICATION]** Write unit tests: SIGINT sets INTERRUPTED, SIGTERM sets INTERRUPTED, current step completes before shutdown
7. **[COMPLETION]** Document signal handling behavior in D-0020/spec.md

**Acceptance Criteria:**
- SIGINT handler registered and sets INTERRUPTED outcome after current step completes (FR-039)
- SIGTERM handler registered and sets INTERRUPTED outcome after current step completes (NFR-003)
- Return contract emitted on INTERRUPTED path (coordinate with T03.10)
- Signal handling documented in .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0020/spec.md

**Validation:**
- `uv run pytest tests/ -k "test_signal_handler"` exits 0
- Evidence: linkable artifact produced at D-0020/spec.md

**Dependencies:** T03.04, T03.05
**Rollback:** TBD (if not specified in roadmap)

---

### Checkpoint: Phase 3 / Tasks T03.05-T03.09

**Purpose:** Verify process management layer is complete: binary detection, status classification, retry, budget tracking, and signal handling all function correctly.

**Checkpoint Report Path:** .dev/releases/current/v2.25-cli-portify-cli/checkpoints/CP-P03-T05-T09.md

**Verification:**
- Claude binary detection fails early with clear error when binary not found
- _determine_status() exhaustively classifies all exit code paths including TIMEOUT (exit 124)
- Signal handlers complete current step and set INTERRUPTED outcome

**Exit Criteria:**
- All process management tasks pass unit tests via `uv run pytest`
- PASS_NO_SIGNAL vs PASS_NO_REPORT retry distinction validated
- TurnLedger budget exhaustion produces HALTED outcome

---

### T03.10 -- Implement Return Contract Emission and Resume Command

| Field | Value |
|---|---|
| Roadmap Item IDs | R-037, R-038, R-039 |
| Why | FR-043 and SC-011 require mandatory return-contract.yaml emission on ALL outcomes (success, failure, dry-run, interrupted). NFR-010 requires resume_command() and NFR-011 requires suggested_resume_budget. |
| Effort | M |
| Risk | Medium |
| Risk Drivers | end-to-end (all code paths) |
| Tier | STRICT |
| Confidence | [████████░░] 88% |
| Requires Confirmation | No |
| Critical Path Override | No |
| Verification Method | Sub-agent (quality-engineer) |
| MCP Requirements | Required: Sequential, Serena |
| Fallback Allowed | No |
| Sub-Agent Delegation | Recommended |
| Deliverable IDs | D-0021 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0021/spec.md

**Deliverables:**
- Return contract emission in `executor.py` producing `return-contract.yaml` on all outcomes, plus `resume_command()` with exact CLI command and `suggested_resume_budget` calculation

**Steps:**
1. **[PLANNING]** Map all outcome paths: success, failure, dry-run, interrupted, halted
2. **[PLANNING]** Define return-contract.yaml schema: outcome, status, completed_steps, remaining_steps, resume_command, suggested_resume_budget
3. **[EXECUTION]** Implement mandatory return-contract.yaml emission on all executor exit paths
4. **[EXECUTION]** Implement `resume_command()` producing exact CLI command string for resumption
5. **[EXECUTION]** Implement `suggested_resume_budget`: `remaining_steps_count * 25` where remaining = steps with PENDING or INCOMPLETE status
6. **[VERIFICATION]** Write unit tests: contract emitted on success, failure, dry-run, interrupted, halted paths (SC-011)
7. **[COMPLETION]** Document return contract schema in D-0021/spec.md

**Acceptance Criteria:**
- `return-contract.yaml` emitted in workdir on ALL outcome paths: success, failure, dry-run, interrupted, halted (SC-011)
- `resume_command()` produces exact `superclaude cli-portify run --resume <step-id>` CLI command
- `suggested_resume_budget` calculated as `remaining * 25` where remaining = count of PENDING or INCOMPLETE steps (NFR-011)
- `suggested_resume_budget` is dynamically calculated from remaining step count — must not use a static/hardcoded turn count
- Return contract schema documented in .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0021/spec.md

**Validation:**
- `uv run pytest tests/ -k "test_return_contract"` exits 0
- Evidence: linkable artifact produced at D-0021/spec.md

**Dependencies:** T03.04, T03.08, T03.09
**Rollback:** TBD (if not specified in roadmap)

---

### T03.11 -- Implement OutputMonitor Baseline and Stall Detection

| Field | Value |
|---|---|
| Roadmap Item IDs | R-040, R-041, R-042 |
| Why | NFR-009 requires OutputMonitor tracking output bytes, growth rate, stall seconds, events, line count. NFR-007 requires execution log skeletons. R-001 requires stall detection with kill action. |
| Effort | M |
| Risk | Medium |
| Risk Drivers | performance (monitoring), system-wide |
| Tier | STRICT |
| Confidence | [████████░░] 85% |
| Requires Confirmation | No |
| Critical Path Override | No |
| Verification Method | Sub-agent (quality-engineer) |
| MCP Requirements | Required: Sequential, Serena |
| Fallback Allowed | No |
| Sub-Agent Delegation | Recommended |
| Deliverable IDs | D-0022 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0022/spec.md

**Deliverables:**
- `monitor.py` baseline with OutputMonitor tracking (output_bytes, growth_rate_bps, stall_seconds, events, line_count, convergence_iteration, findings_count, placeholder_count) and stall detection with kill action
- `logging_.py` skeleton with execution-log.jsonl and execution-log.md emission

**Steps:**
1. **[PLANNING]** Define OutputMonitor fields: output_bytes, growth_rate_bps, stall_seconds, events, line_count, convergence_iteration, findings_count, placeholder_count
2. **[PLANNING]** Define stall detection threshold and kill action flow
3. **[EXECUTION]** Implement `OutputMonitor` class tracking all specified metrics in `monitor.py`
4. **[EXECUTION]** Implement stall detection via `growth_rate_bps`: if rate drops below threshold for configurable duration, trigger kill
5. **[EXECUTION]** Implement `logging_.py` skeleton with `execution-log.jsonl` (machine-readable) and `execution-log.md` (human-readable) file writers
6. **[VERIFICATION]** Write unit tests: monitor tracks metrics correctly, stall detection triggers at threshold
7. **[COMPLETION]** Document monitor architecture in D-0022/spec.md

**Acceptance Criteria:**
- `OutputMonitor` in `monitor.py` tracks output_bytes, growth_rate_bps, stall_seconds, events, line_count, convergence_iteration, findings_count, placeholder_count (NFR-009)
- Stall detection triggers kill action when growth_rate_bps drops below threshold (R-001 mitigation)
- `logging_.py` emits `execution-log.jsonl` and `execution-log.md` skeletons (NFR-007)
- Monitor architecture documented in .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0022/spec.md

**Validation:**
- `uv run pytest tests/ -k "test_output_monitor"` exits 0
- Evidence: linkable artifact produced at D-0022/spec.md

**Dependencies:** T03.04
**Rollback:** TBD (if not specified in roadmap)

---

### T03.12 -- Implement Basic PortifyTUI Start/Stop Lifecycle

| Field | Value |
|---|---|
| Roadmap Item IDs | R-043 |
| Why | NFR-008 requires basic TUI lifecycle (start/stop) to be established before Claude-assisted phases begin (roadmap Phase 4 / tasklist Phase 5) when long-running steps need progress visibility. |
| Effort | S |
| Risk | Low |
| Risk Drivers | None matched |
| Tier | STANDARD |
| Confidence | [███████░░░] 78% |
| Requires Confirmation | No |
| Critical Path Override | No |
| Verification Method | Direct test execution |
| MCP Requirements | Preferred: Sequential |
| Fallback Allowed | Yes |
| Sub-Agent Delegation | None |
| Deliverable IDs | D-0023 |

**Artifacts (Intended Paths):**
- .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0023/evidence.md

**Deliverables:**
- Basic `tui.py` with `PortifyTUI` class implementing start() and stop() lifecycle methods

**Steps:**
1. **[PLANNING]** Review `rich` library API for progress display patterns
2. **[PLANNING]** Define minimal TUI lifecycle: start() initializes display, stop() cleans up
3. **[EXECUTION]** Implement `PortifyTUI` class with `start()` and `stop()` methods using `rich`
4. **[EXECUTION]** Wire TUI lifecycle into executor: start before first step, stop after last step or on error
5. **[VERIFICATION]** Write unit test: TUI starts and stops without error, no hanging processes
6. **[COMPLETION]** Document TUI lifecycle in D-0023/evidence.md

**Acceptance Criteria:**
- `PortifyTUI` class in `tui.py` implements `start()` and `stop()` lifecycle methods using `rich`
- TUI starts before first executor step and stops after last step or on error
- No hanging processes or display artifacts after stop()
- TUI lifecycle documented in .dev/releases/current/v2.25-cli-portify-cli/artifacts/D-0023/evidence.md

**Validation:**
- `uv run pytest tests/ -k "test_tui_lifecycle"` exits 0
- Evidence: linkable artifact produced at D-0023/evidence.md

**Dependencies:** T03.04
**Rollback:** TBD (if not specified in roadmap)

---

### Checkpoint: End of Phase 3

**Purpose:** Verify sequential pipeline runs end-to-end with mocked steps; outcome classification correct; return contract emitted on all code paths; observability baseline captures execution traces.

**Checkpoint Report Path:** .dev/releases/current/v2.25-cli-portify-cli/checkpoints/CP-P03-END.md

**Verification:**
- Executor runs sequentially with mocked steps producing correct outcome classifications (PASS, TIMEOUT, ERROR, INTERRUPTED, HALTED)
- Return contract (return-contract.yaml) emitted on success, failure, dry-run, interrupted, and halted paths (SC-011)
- OutputMonitor baseline captures execution traces for debugging; stall detection triggers kill action

**Exit Criteria:**
- Milestone M2 satisfied: sequential pipeline runs end-to-end; outcome classification correct; return contract emitted on all paths; observability baseline complete
- All 12 tasks (T03.01-T03.12) completed with deliverables D-0012 through D-0023 produced
- Observability baseline dependency met for Claude-assisted phases (roadmap Phase 4 / tasklist Phase 5)
