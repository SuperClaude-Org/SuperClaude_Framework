# Checkpoint CP-P05-T01-T05

**Date**: 2026-03-15
**Phase**: 5 — Conditional --file Fallback Replacement
**Tasks covered**: T05.01 through T05.05 (pre-checkpoint)

## Purpose

Verify all `--file` fallback replacements are correct before adding conditional tests.

## Verification Results

### --file removed from fallback paths

| File | `"--file"` literal in executable code? | Result |
|------|----------------------------------------|--------|
| `src/superclaude/cli/roadmap/executor.py` | No | PASS |
| `src/superclaude/cli/roadmap/remediate_executor.py` | No | PASS |
| `src/superclaude/cli/roadmap/validate_executor.py` | No | PASS |
| `src/superclaude/cli/tasklist/executor.py` | No | PASS |

Verification command:
```
grep -n '"--file"' src/superclaude/cli/roadmap/executor.py
grep -n '"--file"' src/superclaude/cli/roadmap/remediate_executor.py
grep -n '"--file"' src/superclaude/cli/roadmap/validate_executor.py
grep -n '"--file"' src/superclaude/cli/tasklist/executor.py
```
All returned exit 1 (no matches).

### Inline `-p` embedding delivers equivalent content

All four fixes route content through the composed prompt (`step.prompt + "\n\n" + embedded`),
which is passed as the `prompt=` argument to `ClaudeProcess`. The `extra_args` list
is always `[]` in the embedded branch.

`remediate_executor.py` additionally reads the `target_file` content and appends it
as a fenced block titled "## Current File Content".

### Warning behavior for oversized prompts

All three `executor.py`/`validate_executor.py`/`tasklist/executor.py` files now log
a WARNING when `len(composed.encode("utf-8")) > _EMBED_SIZE_LIMIT` but still embed
inline (previously they used `--file` in this case).

### OQ-4 Assessment (T05.05)

The OQ-4 assessment (`--tools default` for non-inheriting executors) is handled
as a separate task. See D-0024.

## Exit Criteria

- [x] All four executor fallback paths use inline embedding
- [x] No remaining `"--file"` usage in executable code paths
- [x] Warning behavior preserved for oversized prompts (just no longer uses `--file`)

## Status

PASS — Proceed to T05.05 (OQ-4 assessment) and T05.06 (conditional tests).
