# D-0008: Extended PortifyConfig Evidence

- 10 new fields added with None defaults: target_input, target_type, command_path, commands_dir, skills_dir, agents_dir, project_root, include_agents, save_manifest_path, component_tree
- All existing 505 tests pass unchanged (backward compatible)
