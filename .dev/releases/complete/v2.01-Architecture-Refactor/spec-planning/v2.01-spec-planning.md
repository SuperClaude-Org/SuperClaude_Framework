# v2.01 Unified Spec Planning Document

**Version**: 1.0.0
**Date**: 2026-02-24
**Status**: Foundation for PRD/Spec
**Scope**: Systemic architectural refactor to enforce agent adherence to custom commands, skills, and agents

---

## 1. Executive Summary

### What v2.01 Is

v2.01 is a systemic architectural refactor of the SuperClaude Framework. Its purpose is to restructure how commands, skills, and agents are authored, loaded, and enforced so that Claude Code agents **reliably follow custom instructions** rather than partially following, silently ignoring, or hallucinating protocol steps.

### The Core Problem

Claude Code agents do not reliably follow custom command and skill specifications. This is not a model capability failure -- it is a **structural failure** in how instructions are authored (monolithic files mixing interface and behavior), loaded (no tiered loading, no dependency chain), and enforced (no CI validation, no runtime gates).

### Why This Release Exists

A rollback incident during the v2.02 sprint proved the structural failures are not theoretical:

- An agent was given a **4-file plan** and executed **68 file changes**
- Context compaction occurred mid-execution, causing the agent to "go rogue"
- The entire change set had to be **rolled back**
- Post-mortem analysis revealed that 4 of 5 modified commands had **zero traceability** (no task, no decision artifact, no evidence record)
- Policy-mandated changes were executed without task tracking, scope boundaries, or post-implementation verification

v2.01 exists to eliminate the structural conditions that enabled this class of failure.

---

## 2. Root Cause Analysis

Seven root causes were identified across multiple independent analyses (root cause extraction, traceability matrix, synthesis artifacts). These have been deduplicated and cross-referenced.

### RC-1: Monolithic Command Files Exceed Agent Attention Budget

**Evidence**: `task-unified.md` was 567 lines containing syntax, flags, examples, tier classification algorithms, compliance checklists, MCP routing tables, sub-agent delegation matrices, and escape hatches -- all auto-loaded into context on every `/sc:task-unified` invocation.

**Mechanism**: At 567+ lines, the agent's attention degrades across the document. Behavioral protocol competes with metadata, examples, and boundary definitions for attention weight.

**Scale**: 6 commands exceed 200 lines: `recommend.md` (1005L), `review-translation.md` (913L), `pm.md` (592L), `task-unified.md` (567L), `spec-panel.md` (435L), `task-mcp.md` (375L).

### RC-2: No Separation Between Interface and Behavioral Specification

**Evidence**: Pre-v2.01, command files contained both "what to invoke" (syntax, flags, usage) and "how to behave" (algorithms, step sequences, YAML specs). The agent has no clear signal about which sections are descriptive context vs. executable instructions.

**Failure mode**: "Silent degradation. If the command's behavioral summary was incomplete or outdated relative to the skill, Claude would guess or hallucinate protocol steps."

### RC-3: No Enforcement Mechanism -- Commands Are Suggestions, Not Contracts

**Evidence**: Before this release, there was no CI validation of command size limits, no check that commands referenced their protocol skills, no validation of skill frontmatter, no build-time verification of command-to-skill relationships, and no runtime enforcement that the agent actually loaded the skill before executing.

**Compounding pattern** ("Visible Instruction Followed, Invisible Prerequisite Skipped"):
- BUG-001: `## Activation` sections (visible) were added to all 5 commands. The `allowed-tools` frontmatter update (invisible prerequisite) was done for only 1 of 5.
- BUG-005: Directory renames (visible) were completed. In-body path references (invisible) were not updated.
- BUG-006: CI check policy (visible) was written. Implementation (invisible) was only 60% complete.

### RC-4: Naming Collision Causes Skill Re-Entry Block

**Evidence**: The Skill tool in Claude Code has a re-entry block: if a skill with the same name as the running command is already active, invoking it again fails silently. When command `adversarial` tries to invoke skill `sc:adversarial`, the invocation is blocked because the system considers the skill "already running."

**Impact**: The `-protocol` suffix convention exists specifically to solve this. This is a Claude Code runtime constraint, not a stylistic choice.

### RC-5: Missing `allowed-tools` Declarations Cause Silent Tool Failures

**Evidence**: BUG-001, independently identified by three separate analysis agents: 4 of 5 commands added mandatory `Skill` invocations in their `## Activation` sections but did NOT add `Skill` to their `allowed-tools` frontmatter.

**Affected files**: `adversarial.md`, `cleanup-audit.md`, `task-unified.md`, `validate-tests.md` (8 files counting both `.claude/` and `src/` copies).

### RC-6: No Loading Order Guarantee or Dependency Chain

**Evidence**: The system has no mechanism to ensure that: (1) the command file is loaded first, (2) the skill is loaded second via explicit invocation, (3) ref files are loaded third via `claude -p`. Without this guarantee, the agent may attempt to execute protocol steps before the skill has been loaded, or may never load the skill at all if context compaction drops the `## Activation` directive.

### RC-7: No CI Validation Means Drift Goes Undetected

**Evidence**: Before v2.01, there was no `make lint-architecture` target. The architecture policy specifies 10 CI checks. Only 6 were implemented (BUG-006). The 4 unimplemented checks represent ongoing drift vectors. Policy without enforcement degrades to suggestion.

### Cross-Reference: Underlying Structural Problems

| Structural Problem | Root Causes | Evidence |
|---|---|---|
| Commands mix dispatch logic with behavioral spec | RC-1, RC-2 | task-unified.md at 567 lines |
| No machine-enforced gate between command and skill | RC-3, RC-6 | `## Activation` is natural language, not enforcement |
| Inconsistent frontmatter creates unpredictable tool availability | RC-5 | Two incompatible frontmatter patterns across 37 commands |
| No loading order guarantee | RC-6 | 3-tier model is design, not enforcement |

---

## 3. Current State Assessment

**Branch**: `feature/v2.01-Roadmap-V3` (commit `9060a65`)

### 3.1 Component Inventory

| Component | Count | Source Location | Dev Copy Location | Sync Status |
|-----------|-------|----------------|-------------------|-------------|
| Commands | 37 | `src/superclaude/commands/` | `.claude/commands/sc/` | IN SYNC |
| Skills | 6 dirs, 33 files | `src/superclaude/skills/` | `.claude/skills/` | **NOT IN SYNC** |
| Agents | 27 | `src/superclaude/agents/` | `.claude/agents/` | IN SYNC |

### 3.2 Commands Over 200 Lines (Extraction Candidates)

| Command | Lines | Has Paired Skill? | Extraction Priority |
|---------|-------|-------------------|---------------------|
| `recommend.md` | 1005 | No | High |
| `review-translation.md` | 913 | No | High |
| `pm.md` | 592 | No | Medium |
| `task-unified.md` | 567 | Yes (`sc-task-unified-protocol`) | Medium |
| `spec-panel.md` | 435 | No | Medium |
| `task-mcp.md` | 375 | No | Medium |

### 3.3 Activation Section Coverage

| Metric | Count |
|--------|-------|
| Commands with `## Activation` section | **1** (`roadmap.md`) |
| Commands that need `## Activation` (have paired skill) | **5** |
| Commands with `Skill` in `allowed-tools` | **0** |
| Commands using `allowed-tools` frontmatter at all | **1** (`roadmap.md`) |
| Commands using `mcp-servers` frontmatter | **36** |

### 3.4 Skill Sync Status

The `.claude/skills/` directories for `-protocol` suffixed skills contain **only empty subdirectory structures** (from git renames that moved files in `src/` but `make sync-dev` was not run).

| Skill Directory | src/ Files | .claude/ Files | Status |
|---|---|---|---|
| `confidence-check` | 3 | 2 | MISMATCH |
| `sc-adversarial-protocol` | 6 | 0 | EMPTY |
| `sc-cleanup-audit-protocol` | 12 | 0 | EMPTY |
| `sc-roadmap-protocol` | 7 | 0 | EMPTY |
| `sc-task-unified-protocol` | 2 | 0 | EMPTY |
| `sc-validate-tests-protocol` | 3 | 0 | EMPTY |

### 3.5 Broken References

- `roadmap.md` Activation section references old path `src/superclaude/skills/sc-roadmap/SKILL.md` (should be `sc-roadmap-protocol`)
- `roadmap SKILL.md` Wave 0 step 5 references `sc-adversarial/` (old path)
- `.claude/skills/` `-protocol` directories are empty shells

### 3.6 Naming Inconsistencies

- `confidence-check` skill lacks `sc-` prefix and `-protocol` suffix (standalone skill)
- Two task-related commands: `task.md` (115L) and `task-unified.md` (567L)
- Two frontmatter patterns: Pattern A (36 commands with `mcp-servers`) vs. Pattern B (1 command with `allowed-tools`)

### 3.7 Build System

| Target | Exists? | Notes |
|--------|---------|-------|
| `make lint-architecture` | **NO** | Must be created |
| `make sync-dev` | YES | Contains obsolete skill-skip heuristic |
| `make verify-sync` | YES | Contains obsolete skill-skip heuristic |

---

## 4. Target Architecture

### 4.1 The 3-Tier Loading Model

> **"Commands are doors. Skills are rooms. Refs are drawers."**

| Tier | Component | Loading Mechanism | Max Size |
|------|-----------|-------------------|----------|
| **0** -- Command | Entry point: metadata, usage, examples, activation directive, boundaries | Auto-loaded on `/sc:<name>` invocation | **<=150 lines** |
| **1** -- Protocol Skill | Full behavioral protocol: steps, agent dispatch, YAML specs, error handling | Agent invokes `Skill sc:<name>-protocol` | Unlimited |
| **2** -- Refs | Detailed algorithms, templates, scoring rubrics | Loaded via `claude -p` script per step | Unlimited |

#### Loading Flow

```
User types:     /sc:adversarial --compare file1.md,file2.md
                        |
                        v
Tier 0:         adversarial.md auto-loaded (<=150 lines)
                        |
                        v (## Activation section says: Invoke Skill sc:adversarial-protocol)
                        |
Tier 1:         Agent calls Skill tool -> sc:adversarial-protocol/SKILL.md loaded
                        |
                        v (Step 3 says: Load refs/scoring-protocol.md via claude -p)
                        |
Tier 2:         claude -p injects scoring-protocol.md into sub-agent context
```

### 4.2 Command File Contract (Tier 0) -- Verbatim Template

```markdown
---
name: <name>
description: "<one-line description>"
category: <development|analysis|quality|testing|documentation|meta>
complexity: <basic|moderate|advanced>
mcp-servers: [<server-list>]
personas: [<persona-list>]
allowed-tools: <tool-list including Skill>
---

# /sc:<name> - <Title>

## Usage
<invocation patterns with code blocks>

## Arguments
<flags/options table>

## Examples
<3-5 concrete usage examples>

## Activation

**MANDATORY**: Before executing any protocol steps, invoke:
> Skill sc:<name>-protocol

Do NOT proceed with protocol execution using only this command file.
The full behavioral specification is in the protocol skill.

## Behavioral Summary
<<=5 sentences describing what the pipeline does at a high level>

## Boundaries
**Will:** <bullet list>
**Will Not:** <bullet list>

## Related Commands
<optional: table of related commands>
```

#### Hard Constraints

- **<=150 lines** total
- **NO** protocol YAML blocks, step definitions, or scoring algorithms
- **MUST** have `## Activation` section if a protocol skill exists
- `## Activation` MUST name the exact skill: `sc:<name>-protocol`
- `## Activation` MUST include the "Do NOT proceed" warning
- `## Behavioral Summary` is <=5 sentences (high-level only)
- `Skill` MUST appear in `allowed-tools` frontmatter

#### Command-Only Files (No Skill)

Commands without a matching protocol skill omit `## Activation`, may contain full behavioral instructions inline, and are subject to: WARN at 200 lines, ERROR at 500 lines.

### 4.3 Protocol Skill Contract (Tier 1) -- Verbatim Template

```markdown
---
name: sc:<name>-protocol
description: "<description>"
allowed-tools: <tool list including Skill if cross-invocation needed>
argument-hint: "<usage hint>"
---

# sc:<name>-protocol - <Title>

## Purpose
<what this protocol does and why>

## Triggers
<when this skill is invoked>

## Protocol Steps
### Step 1: <Name>
<full specification with YAML blocks>
### Step 2: <Name>
...

## Configurable Parameters
<flags, thresholds, defaults>

## Agent Delegation
<which agents are spawned, their roles, dispatch config>

## MCP Integration
<which MCP servers, which steps, circuit breakers>

## Error Handling
<error matrix with recovery actions>

## Return Contract
<output fields for programmatic integration>

## Boundaries
**Will:** / **Will Not:**
```

#### Hard Constraints

- **MUST** have frontmatter with `name`, `description`, `allowed-tools`
- `name` field MUST match the directory name pattern: `sc:<name>-protocol`
- **MUST** define all behavioral logic (the command file contains NONE)
- References to Tier 2 content use: `**Ref**: Load refs/<filename>.md via claude -p before executing this step.`
- No size limit, but prefer splitting deep detail into refs/ for context efficiency

### 4.4 Ref File Contract (Tier 2) -- Verbatim Template

```markdown
# <Title>

## Purpose
<what this ref contains and when to load it>

## Content
<the actual specification, algorithm, template, or rubric>

---
*Reference document for sc:<name>-protocol skill*
*Source: <original source attribution>*
```

#### Hard Constraints

- **MUST** be independently useful (no dangling references requiring SKILL.md context)
- **MUST** have a `## Purpose` section explaining when to load it
- Loaded via `claude -p` with ref content as `--append-system-prompt`
- One ref per concern (do not combine scoring + templates in one file)

### 4.5 Naming Conventions

| Component | Name Pattern | Example | Location |
|-----------|-------------|---------|----------|
| Command | `<name>` | `adversarial` | `commands/<name>.md` |
| Protocol Skill (directory) | `sc-<name>-protocol` | `sc-adversarial-protocol` | `skills/sc-<name>-protocol/` |
| Protocol Skill (frontmatter `name`) | `sc:<name>-protocol` | `sc:adversarial-protocol` | SKILL.md `name:` field |
| Ref files | descriptive name | `scoring-protocol.md` | `skills/sc-<name>-protocol/refs/` |
| Standalone Skill | no `sc-` prefix, no `-protocol` suffix | `confidence-check` | `skills/<name>/SKILL.md` |

**Rules**:
- Protocol skills MUST end in `-protocol` (both directory and frontmatter name)
- Protocol skill directories MUST be prefixed with `sc-` and suffixed with `-protocol`
- Standalone skills (no matching command) do NOT use the `-protocol` suffix
- Command names NEVER include `-protocol`
- Directory names use hyphens (`sc-adversarial-protocol`); frontmatter `name:` uses colons (`sc:adversarial-protocol`) -- directories cannot contain colons

### 4.6 The `-protocol` Suffix Rationale

The `-protocol` suffix exists to solve a Claude Code runtime constraint: the **Skill tool re-entry block**. When `/sc:adversarial` triggers, the skill `sc:adversarial` is marked as running. If the command tries to invoke `Skill sc:adversarial`, it is blocked because the system considers the skill "already running." Using `sc:adversarial-protocol` avoids this entirely.

This is a runtime constraint, not a stylistic choice. Removing the suffix would break the command-to-skill activation chain.

---

## 5. CI Enforcement Requirements

### 5.1 All 10 Policy-Defined Checks

| # | Check | Severity | Rule |
|---|-------|----------|------|
| 1 | Bidirectional link (cmd->skill) | ERROR | Command with `## Activation` referencing a skill -> that skill directory MUST exist |
| 2 | Bidirectional link (skill->cmd) | ERROR | Skill directory `sc-*-protocol` -> command `<name>.md` MUST exist |
| 3 | Command size (warn) | WARN | Command file >200 lines |
| 4 | Command size (error) | ERROR | Command file >500 lines |
| 5 | Inline protocol in command | ERROR | Command with matching `-protocol` skill contains YAML code blocks >20 lines |
| 6 | Activation section present | ERROR | Command with matching `-protocol` skill directory missing `## Activation` section |
| 7 | Activation references correct skill | ERROR | `## Activation` section does not contain `Skill sc:<name>-protocol` |
| 8 | Skill frontmatter complete | ERROR | SKILL.md missing `name`, `description`, or `allowed-tools` in frontmatter |
| 9 | Skill naming consistency | ERROR | Skill directory `sc-*-protocol/` but SKILL.md `name` field doesn't end in `-protocol` |
| 10 | Sync integrity | ERROR | Files in `src/` not matching `.claude/` (delegated to existing `verify-sync`) |

### 5.2 Implementation Status

| Check | Status | Implementation Notes |
|-------|--------|---------------------|
| 1 | **DESIGNED** | Grep for `## Activation`, extract skill name, verify directory exists |
| 2 | **DESIGNED** | Enumerate `sc-*-protocol/` dirs, derive command name, verify file exists |
| 3 + 4 | **DESIGNED** | Combined into single check with two thresholds (`wc -l`) |
| 5 | **NEEDS DESIGN** | Requires YAML block detection in markdown (more complex than simple grep/wc) |
| 6 | **DESIGNED** | Grep for `## Activation` in paired commands |
| 7 | **NEEDS DESIGN** | Partially covered by Check 1 but does NOT validate the reference matches expected naming pattern |
| 8 | **DESIGNED** | Grep for `name:`, `description:`, `allowed-tools:` in SKILL.md frontmatter |
| 9 | **DESIGNED** | Match directory suffix against frontmatter `name` field |
| 10 | **DELEGATED** | Existing `make verify-sync` target |

### 5.3 Verification Patterns (Reusable Bash Scripts)

```bash
# Check 1/2: Bidirectional links
for f in src/superclaude/commands/*.md; do
  if grep -q "## Activation" "$f"; then
    skill_ref=$(grep "Skill sc:" "$f" | sed 's/.*Skill sc:/sc-/' | sed 's/-protocol.*/-protocol/')
    test -d "src/superclaude/skills/$skill_ref/" && echo "PASS: $f" || echo "FAIL: $f"
  fi
done

# Check 3/4: Command size limits
for f in src/superclaude/commands/*.md; do
  lines=$(wc -l < "$f")
  if [ "$lines" -gt 500 ]; then echo "ERROR: $f ($lines lines)"
  elif [ "$lines" -gt 200 ]; then echo "WARN: $f ($lines lines)"
  fi
done

# Check 6: Activation section present in paired commands
for d in src/superclaude/skills/sc-*-protocol/; do
  cmd_name=$(basename "$d" | sed 's/^sc-//' | sed 's/-protocol$//')
  grep -q "## Activation" "src/superclaude/commands/$cmd_name.md" && echo "PASS: $cmd_name" || echo "FAIL: $cmd_name"
done

# Check 8: Skill frontmatter validation
for d in src/superclaude/skills/sc-*-protocol/SKILL.md; do
  for field in "name:" "description:" "allowed-tools:"; do
    grep -q "$field" "$d" || echo "FAIL: $d missing $field"
  done
done

# Check 9: Naming consistency
for d in src/superclaude/skills/sc-*-protocol/SKILL.md; do
  name=$(grep "^name:" "$d" | head -1)
  echo "$name" | grep -q ".*-protocol" && echo "PASS" || echo "FAIL: $d -> $name"
done

# Post-rename stale reference check
grep -rn "sc-adversarial/" src/ .claude/ docs/ --include="*.md" || echo "CLEAN"
grep -rn "sc-cleanup-audit/" src/ .claude/ docs/ --include="*.md" || echo "CLEAN"
grep -rn "sc-roadmap/" src/ .claude/ docs/ --include="*.md" || echo "CLEAN"
grep -rn "sc-task-unified/" src/ .claude/ docs/ --include="*.md" || echo "CLEAN"
grep -rn "sc-validate-tests/" src/ .claude/ docs/ --include="*.md" || echo "CLEAN"
```

### 5.4 Makefile Integration

- `lint-architecture` added to `.PHONY` declaration
- Added to `help` target under "Component Sync" section
- Inserted after `verify-sync` target in Makefile
- Exit behavior: errors > 0 -> `exit 1`; warnings only -> `exit 0`; clean -> `exit 0`
- References `docs/architecture/command-skill-policy.md` in failure messages

### 5.5 Validation Pipeline

```
sync-dev          -->  verify-sync       -->  lint-architecture
(copy src->.claude)    (diff check)           (policy enforcement)
```

---

## 6. Migration Scope

### 6.1 Activation Section Additions (36 of 37 commands need assessment)

Of 37 commands, only `roadmap.md` has an `## Activation` section (and it references the old path). The 5 commands with paired skills all need activation sections:

| Command | Current State | Action Required |
|---------|--------------|-----------------|
| `adversarial.md` (112L) | No Activation | Add `## Activation` section |
| `cleanup-audit.md` (93L) | No Activation | Add `## Activation` section |
| `roadmap.md` (76L) | Has Activation (old path) | Rewrite to use `Skill sc:roadmap-protocol` |
| `task-unified.md` (567L) | No Activation | Add `## Activation` + extract 461 lines to skill |
| `validate-tests.md` (102L) | No Activation | Add `## Activation` section |

### 6.2 Content Extraction (6 Over-200-Line Commands)

| Command | Lines | Has Skill? | Action |
|---------|-------|-----------|--------|
| `recommend.md` | 1005 | No | Create `sc-recommend-protocol` + extract |
| `review-translation.md` | 913 | No | Create `sc-review-translation-protocol` + extract |
| `pm.md` | 592 | No | Create `sc-pm-protocol` + extract |
| `task-unified.md` | 567 | Yes | Extract behavioral content to existing skill |
| `spec-panel.md` | 435 | No | Create `sc-spec-panel-protocol` + extract |
| `task-mcp.md` | 375 | No | Create `sc-task-mcp-protocol` + extract |

**Extraction exemplar**: `task-unified.md` extraction reduced 567 lines to 106 lines (81% extraction). Content extracted: tier algorithms, compliance checklists, MCP matrix, delegation tables, escape hatches, metrics.

### 6.3 Skill Renames (Already Executed in Git)

The 5 skill renames from `sc-<name>` to `sc-<name>-protocol` are **already staged** in git on the current branch:

| Old Directory | New Directory | SKILL.md `name` Update |
|---|---|---|
| `sc-adversarial` | `sc-adversarial-protocol` | `sc:adversarial` -> `sc:adversarial-protocol` |
| `sc-cleanup-audit` | `sc-cleanup-audit-protocol` | `cleanup-audit` -> `sc:cleanup-audit-protocol` |
| `sc-roadmap` | `sc-roadmap-protocol` | `sc:roadmap` -> `sc:roadmap-protocol` |
| `sc-task-unified` | `sc-task-unified-protocol` | `sc-task-unified` -> `sc:task-unified-protocol` |
| `sc-validate-tests` | `sc-validate-tests-protocol` | `sc-validate-tests` -> `sc:validate-tests-protocol` |

### 6.4 Frontmatter Standardization

All 5 paired commands need:
1. `Skill` added to `allowed-tools` frontmatter
2. `allowed-tools` field added if only `mcp-servers` exists
3. Consistent frontmatter schema across all 37 commands

### 6.5 Makefile Changes

| Change | Lines Affected | Priority |
|--------|---------------|----------|
| Remove skill-skip heuristic from `sync-dev` | 4 lines removed | HIGH |
| Remove skill-skip heuristic from `verify-sync` | 5 lines removed | HIGH |
| Add `lint-architecture` target | ~113 lines added | HIGH |
| Update `.PHONY` declaration | 1 line modified | HIGH |
| Update `help` target | 1 line added | LOW |

### 6.6 Sync Requirements

After all `src/` changes are complete:
1. `make sync-dev` to populate `.claude/` from `src/`
2. `make verify-sync` to confirm parity
3. `make lint-architecture` to validate conventions

### 6.7 Atomic Change Groups

These files MUST be changed together. Changing any file without its group members produces broken references.

**Group A: Per-Command Unit** (repeat for each of 5 paired commands):

| File | Change |
|------|--------|
| `src/superclaude/commands/{name}.md` | Add/rewrite `## Activation` section |
| `.claude/commands/sc/{name}.md` | Identical change (sync copy) |
| `src/superclaude/skills/sc-{name}-protocol/SKILL.md` | Frontmatter update |
| `.claude/skills/sc-{name}-protocol/` | New dev copy directory |

**Group B: Makefile Enforcement**:

| File | Change |
|------|--------|
| `Makefile` (sync-dev) | Remove skip logic |
| `Makefile` (verify-sync) | Remove skip logic |
| `Makefile` (lint-architecture) | Add new target |
| `Makefile` (.PHONY, help) | Reference new target |

### 6.8 Recreation Dependency DAG

```
Layer 0 (Foundation):
  docs/architecture/command-skill-policy.md

Layer 1 (Skill Renames - DONE):
  src/superclaude/skills/sc-{all 5}-protocol/

Layer 2 (Command Updates - depend on Layer 1):
  src/superclaude/commands/{5 paired commands}

Layer 3 (Dev Copies - depend on Layer 1 + 2):
  .claude/commands/sc/{all 5} + .claude/skills/sc-{all 5}-protocol/

Layer 4 (Build System - depends on Layer 1 naming):
  Makefile: sync-dev + verify-sync + lint-architecture

Layer 5 (Validation - depends on all above):
  make sync-dev && make verify-sync && make lint-architecture
```

No circular dependencies. Clean DAG.

---

## 7. Process Requirements (Lessons Learned)

These requirements are derived from the rollback incident post-mortem and traceability analysis.

### 7.1 Every File Modification Must Trace to a Task

In the prior sprint, 4 of 5 modified commands had zero traceability (no task, no decision artifact, no evidence record). Policy mandates are not a substitute for task tracking.

**Rule**: If a policy drives changes beyond the sprint scope, those changes must either (a) be added as explicit tasks in the current sprint, or (b) be deferred to a dedicated sprint.

### 7.2 All Files Must Be Enumerated Before Sprint Start

The prior sprint planned 4 files and touched 68. Task decomposition must account for the full scope of policy mandates before sprint start.

**Rule**: Sprint plans must enumerate all files implied by policy mandates. Scope boundaries must be explicit: "this sprint executes Phase 1 steps 1-3 for skills X and Y only."

### 7.3 Policy Mandates Must Decompose Into Discrete Tasks

Of 5 skill renames, only 1 had an associated sprint task. The other 4 were driven solely by the architecture policy document with no task decomposition.

**Rule**: When a pattern applies to N items, create N explicit tasks (or one explicit "apply to all N" task with a checklist). Never assume one example will be generalized.

### 7.4 Content Extraction Is a Formally Tracked Operation

The `task-unified.md` 81% content extraction was the single largest and riskiest change, yet it had no task, no decision artifact, and no verification.

**Rule**: Content extraction requires: (1) explicit task tracking, (2) pre-extraction inventory, (3) post-extraction verification that extracted content landed in the skill, (4) validation that the command's activation section correctly delegates.

### 7.5 Enforcement BEFORE Migration

The architecture policy's enforcement checks must be implemented as actual runnable validation before any migration work begins. Policy without enforcement produced 4/5 commands modified without any automated verification.

**Rule**: `make lint-architecture` must be implemented and passing before commands are refactored.

### 7.6 Compliance Tier Classification for Executable .md Files

Executable `.md` files (commands, SKILL.md, agent definitions, protocol specifications) are NOT exempt from compliance enforcement, even though the compliance system's auto-classifier has a `*.md` EXEMPT booster.

**Classification rule**:
- `.md` files in `docs/`, `README*`, `CHANGELOG*` -> EXEMPT tier
- `.md` files defining executable behavior (skills, commands, agents, protocols) -> STANDARD minimum, STRICT if multi-file or security-adjacent

### 7.7 Verification Claims Must Be Checked Against Codebase

One synthesis agent incorrectly reported `lint-architecture` as "not yet implemented" because it read the policy document's self-reported status rather than checking git. Analysis processes must verify claims against actual codebase state.

---

## 8. Known Bugs to Fix

### 8.1 v2.01-Relevant Bugs

| Bug ID | Severity | Summary | Fix |
|--------|----------|---------|-----|
| **BUG-001** | HIGH | 4 of 5 commands missing `Skill` in `allowed-tools` after adding `## Activation` | Add `Skill` to `allowed-tools` in `adversarial.md`, `cleanup-audit.md`, `task-unified.md`, `validate-tests.md` |
| **BUG-004** | MEDIUM | Architecture policy duplication (`docs/architecture/` vs `src/superclaude/ARCHITECTURE.md`), no canonical authority | Designate `docs/architecture/` as canonical, remove or replace duplicate with pointer |
| **BUG-005** | MEDIUM | Stale path in roadmap SKILL.md (`sc-adversarial/` instead of `sc-adversarial-protocol/`) | Update in-body path reference |
| **BUG-006** | LOW | `lint-architecture` implements 6 of 10 policy-specified checks | Implement remaining checks #5 and #7 |
| **BUG-009** | LOW | Pattern inconsistency: some skill steps delegate to refs, some inline | Establish consistent delegation pattern |

### 8.2 Systemic Patterns These Bugs Reveal

| Pattern | Bugs | Prevention |
|---------|------|------------|
| "Visible instruction followed, invisible prerequisite skipped" | BUG-001 | Make ALL co-requisites explicit in every instruction |
| Stale path references after rename | BUG-005 | Post-rename grep sweep across ALL files |
| Policy-enforcement completeness gap | BUG-006 | Implementation tracker per policy item |
| Canonical source ambiguity | BUG-004 | Single source of truth, pointer files for discoverability |

### 8.3 Additional Issues

| Issue | Severity | Description |
|-------|----------|-------------|
| `roadmap.md` Activation references old path | HIGH | `src/superclaude/skills/sc-roadmap/SKILL.md` instead of protocol skill invocation |
| `task-unified.md` frontmatter `name: task` | MEDIUM | Mismatches skill name `sc:task-unified-protocol` |
| `validate-tests.md` has no YAML frontmatter | MEDIUM | Needs complete frontmatter added |
| `.claude/skills/` protocol dirs are empty | HIGH | Need `make sync-dev` after all src/ changes |

---

## 9. Gaps and Open Questions

### 9.1 Policy Gaps (from policy extraction)

| # | Gap | Priority | Notes |
|---|-----|----------|-------|
| 1 | `claude -p` Tier 2 ref loader not designed | HIGH | Policy references `claude -p` for loading ref files but provides no script or spec. Full 3-tier model is incomplete without this. |
| 2 | Cross-skill invocation not specified | HIGH | No specification for how one protocol skill invokes another. Critical for pipeline composition (e.g., roadmap -> adversarial). T01.01 probe returned TOOL_NOT_AVAILABLE. |
| 3 | `__init__.py` purpose undefined | LOW | Present in skill directories but policy never explains purpose, required content, or if mandatory. |
| 4 | `argument-hint` frontmatter -- required or optional? | LOW | Present in Skill Contract template but not validated by CI checks. |
| 5 | Standalone skill contract not fully specified | MEDIUM | Policy says standalone skills don't use `-protocol` suffix but provides no contract template. |
| 6 | Command frontmatter not CI-validated | MEDIUM | Only skill frontmatter is checked (Check 8). No check validates command frontmatter completeness. |
| 7 | No rollback procedure documented | MEDIUM | Migration checklist defines forward path only. |
| 8 | Sync direction ambiguity | LOW | Historical confusion about which direction sync flows (now clarified: `src/` -> `.claude/`). |

### 9.2 Unresolved Architectural Items

| Item | Priority | Notes |
|------|----------|-------|
| Runtime scope control (file-set limits) | CRITICAL | No mechanism to declare "this command operates on files X, Y, Z and ONLY those files." Prevents "68 files changed" class of failure. **NOT YET DESIGNED.** |
| Context compaction resilience | HIGH | When context is compacted, behavioral directives may be summarized away. The 3-tier model partially addresses this but has no explicit design. |
| Plan validation gate | HIGH | No checkpoint requiring the agent to present its plan and receive approval before executing. |

### 9.3 The Critical Unsolved Problem: Runtime Scope Control

This is the most significant gap. None of the analysis documents propose a concrete design for runtime scope control. The rollback incident's primary symptom -- an agent changing 68 files when 4 were planned -- cannot be prevented by the current architecture. The 3-tier loading model addresses instruction quality but not execution boundaries.

Possible approaches (not yet evaluated):
- File-set declarations in command/skill boundaries
- Plan presentation gates before execution
- Boundary enforcement via tool permissions
- Atomic change group declarations

---

## 10. Risk Assessment

### 10.1 Partial Application Risks

| Partial Application | What Breaks | Severity |
|---------------------|-------------|----------|
| Commands updated, skills NOT renamed | `Skill sc:*-protocol` invocations fail (skill directories don't exist under new names) | CRITICAL |
| Skills renamed, commands NOT updated | Old commands reference old skill names or use implicit loading | CRITICAL |
| `lint-architecture` added, skills NOT renamed | Check 2 and Check 6 fail (expect `-protocol` directories) | HIGH |
| Makefile skip-heuristic removed, skills NOT renamed | `sync-dev` creates redundant `.claude/skills/sc-*` directories | MEDIUM |
| `.claude/` updated, `src/` NOT updated | `make verify-sync` fails; source-of-truth diverges | HIGH |
| `src/` updated, `.claude/` NOT updated | Claude Code reads stale skill content; runtime behavior mismatch | HIGH |

### 10.2 Test Coverage Gaps

| Change | Test Coverage | Gap Severity |
|--------|---------------|--------------|
| Command-skill activation flow | NONE -- no test verifies `Skill sc:X-protocol` loads the skill | HIGH |
| `allowed-tools` enforcement | NONE -- no test verifies tool whitelist is respected | HIGH |
| `task-unified.md` content extraction | NONE -- no test verifies 106-line command still works | HIGH |
| Skill directory renames | PARTIAL -- `lint-architecture` validates naming | MEDIUM |
| Makefile `sync-dev` behavior change | PARTIAL -- `verify-sync` checks parity | MEDIUM |
| `lint-architecture` correctness | NONE -- no test verifies the 6 checks produce correct results | MEDIUM |

### 10.3 Recommended Test Additions

1. **Integration test**: Invoke each command and verify skill activation loads the correct skill
2. **Regression test**: Verify `task-unified` performs tier classification correctly after extraction
3. **Positive lint test**: Run `make lint-architecture` against the final tree and verify exit 0
4. **Negative lint test**: Verify `make lint-architecture` fails when a skill is missing or `## Activation` is absent
5. **`allowed-tools` test**: Verify `Skill` tool works for commands where it is/isn't in `allowed-tools`

---

## 11. References

### 11.1 Source Extraction Files (12)

| # | File | Content Focus |
|---|------|--------------|
| 1 | `extract-synthesis-planning.md` | 3-Tier model, naming conventions, CI requirements, compliance tier framework |
| 2 | `extract-synthesis-artifacts.md` | Spec quality patterns, adversarial QA, root cause patterns from artifact analysis |
| 3 | `extract-synthesis-framework.md` | Command-skill decoupling, `-protocol` naming, `## Activation` pattern, atomic change groups, test gaps |
| 4 | `extract-instructions-master-renames.md` | Skill rename procedure, verification checklists, rollback procedures |
| 5 | `extract-instructions-commands-makefile.md` | Activation template, content extraction pattern, `lint-architecture` spec, skill-skip removal |
| 6 | `extract-instructions-planning-artifacts.md` | Sprint artifact structure, evidence chains, deduplication, recreation difficulty classification |
| 7 | `extract-context-commands-skills.md` | Traceability gaps, scope expansion, `allowed-tools` inconsistency, command-skill linkage |
| 8 | `extract-context-makefile-adversarial.md` | 10-check vs 6-check gap, sync-dev requirements, adversarial review process |
| 9 | `extract-context-traceability.md` | Consolidated bug list, cross-category dependency model, systemic quality gaps, 5 root causes |
| 10 | `extract-policy-command-skill.md` | Verbatim policy: 3-tier model, contracts, naming, CI checks, migration checklist, gaps |
| 11 | `extract-root-cause-analysis.md` | 7 root causes with evidence, structural problems, proposed solutions, gap analysis |
| 12 | `extract-current-state-inventory.md` | 37 commands, 6 skills, 27 agents: sizes, sync status, broken references, naming inconsistencies |

### 11.2 Original Source Locations

| Document | Location |
|----------|----------|
| Rollback analysis (full) | `.dev/releases/backlog/v2.02-Roadmap-v3/rollback-analysis/` |
| Architecture policy | `docs/architecture/command-skill-policy.md` |
| Architecture policy (duplicate) | `src/superclaude/ARCHITECTURE.md` |
| Current state inventory | Branch `feature/v2.01-Roadmap-V3`, commit `9060a65` |

### 11.3 Line Count Constraints Summary

| Component | Soft Limit | Hard Limit | Action |
|-----------|-----------|------------|--------|
| Command with protocol skill | 150 lines | 150 lines | ERROR if exceeded |
| Command without skill | 200 lines (WARN) | 500 lines (ERROR) | Must split at 500+ |
| Protocol Skill (SKILL.md) | None | Unlimited | Prefer splitting to refs |
| Ref files | None | Unlimited | One concern per file |
| YAML blocks in commands (with skill) | N/A | 20 lines | ERROR if exceeded |

---

*Unified from 12 extraction files for v2.01 Architecture Refactor specification planning.*
